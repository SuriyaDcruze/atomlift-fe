import { StyleSheet, Platform, StatusBar, Dimensions } from 'react-native';

// Get screen dimensions for mobile height calculations
const { height: SCREEN_HEIGHT } = Dimensions.get('window');
const WEB_SCREEN_HEIGHT = Platform.OS === 'web' ? (typeof window !== 'undefined' ? window.innerHeight : 800) : SCREEN_HEIGHT;

// Get status bar height for consistent header padding
const getStatusBarHeight = (): number => {
  if (Platform.OS === 'ios') {
    return StatusBar.currentHeight || 44;
  } else {
    return StatusBar.currentHeight || 24;
  }
};

const HEADER_PADDING_TOP = Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5;

export const globalStyles = StyleSheet.create({
  // Login Screen Styles
  loginContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
  },
  loginKeyboardView: {
    flex: 1,
  },
  loginContent: {
    flexGrow: 1,
    justifyContent: 'center',
    paddingHorizontal: 30,
    paddingVertical: 16,
    width: '100%',
    alignSelf: 'stretch',
  },
  loginHeader: {
    alignItems: 'center',
    marginBottom: 40,
  },
  loginTitle: {
    fontSize: 32,
    fontWeight: 'bold',
    color: '#2c3e50',
    marginBottom: 8,
  },
  loginSubtitle: {
    fontSize: 16,
    color: '#7f8c8d',
  },
  loginForm: {
    width: '100%',
    alignSelf: 'stretch',
  },
  loginInputContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    borderRadius: 12,
    marginBottom: 16,
    paddingHorizontal: 16,
    paddingVertical: 4,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.05,
    shadowRadius: 2,
    elevation: 1,
  },
  loginInputIcon: {
    marginRight: 12,
  },
  loginInput: {
    flex: 1,
    fontSize: 16,
    color: '#2c3e50',
    paddingVertical: 16,
    minHeight: 48,
  },
  loginEyeIcon: {
    padding: 4,
  },
  loginButton: {
    backgroundColor: '#3498db',
    borderRadius: 12,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 8,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  loginButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },
  loginForgotPassword: {
    alignItems: 'center',
    marginTop: 20,
  },
  loginForgotPasswordText: {
    color: '#3498db',
    fontSize: 16,
  },
  loginMobileNumberDisplay: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#fff',
    borderRadius: 12,
    marginBottom: 16,
    paddingHorizontal: 16,
    paddingVertical: 16,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 1,
    },
    shadowOpacity: 0.05,
    shadowRadius: 2,
    elevation: 1,
  },
  loginMobileNumberText: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
  },
  loginChangeNumberText: {
    color: '#3498db',
    fontSize: 16,
    fontWeight: '500',
  },
  loginResendContainer: {
    alignItems: 'center',
    marginTop: 20,
  },
  loginTimerText: {
    color: '#7f8c8d',
    fontSize: 16,
  },
  loginResendText: {
    color: '#3498db',
    fontSize: 16,
    fontWeight: '500',
  },

  // Home Screen Styles
  homeContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  homeHeader: {
    backgroundColor: '#3498db',
    paddingTop: Platform.OS === 'web' ? 20 : Math.max(StatusBar.currentHeight || 0, 20) + 8,
    paddingBottom: Platform.OS === 'web' ? 16 : 12,
    paddingHorizontal: 12,
  },
  homeHeaderBottom: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  homeContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    paddingBottom: 50,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  homeMenuGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
    marginBottom: 30,
    width: '100%',
    alignSelf: 'stretch',
  },
  homeMenuCard: {
    width: '48%',
    backgroundColor: '#fff',
    borderRadius: 12,
    padding: 16,
    alignItems: 'center',
    marginBottom: 16,
    minHeight: 140,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  homeIconContainer: {
    width: 60,
    height: 60,
    borderRadius: 30,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 12,
  },
  homeMenuTitle: {
    fontSize: 16,
    fontWeight: '600',
    color: '#2c3e50',
    textAlign: 'center',
  },
  homeActionButtons: {
    marginBottom: 20,
    width: '100%',
    alignSelf: 'stretch',
  },
  homeActionButton: {
    backgroundColor: '#3498db',
    borderRadius: 12,
    paddingVertical: 16,
    alignItems: 'center',
    marginBottom: 12,
    minHeight: 48,
    width: '100%',
    alignSelf: 'stretch',
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  homeActionButtonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
  },
  homeStatusContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 20,
  },
  homeStatusText: {
    fontSize: 14,
    color: '#2c3e50',
    marginRight: 8,
  },
  homeModalOverlay: {
    flex: 1,
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
  },
  homeDrawerContainer: {
    width: '85%',
    height: '100%',
    backgroundColor: '#fff',
    elevation: 5,
    shadowColor: '#000',
    shadowOffset: {
      width: 2,
      height: 0,
    },
    shadowOpacity: 0.25,
    shadowRadius: 3.84,
  },
  homeModalBackdrop: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
  },

  // Drawer Styles
  drawerContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
  },
  drawerHeader: {
    backgroundColor: '#3498db',
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20),
    paddingBottom: Platform.OS === 'web' ? 16 : 14,
    paddingHorizontal: Platform.OS === 'web' ? 20 : 16,
    width: '100%',
  },
  drawerTimeContainer: {
    marginBottom: 12,
    width: '100%',
  },
  drawerTimeText: {
    color: '#fff',
    fontSize: Platform.OS === 'web' ? 16 : 15,
    fontWeight: '500',
  },
  drawerUserInfo: {
    flexDirection: 'row',
    alignItems: 'flex-start',
    width: '100%',
  },
  drawerUserIconContainer: {
    width: Platform.OS === 'web' ? 50 : 44,
    height: Platform.OS === 'web' ? 50 : 44,
    borderRadius: Platform.OS === 'web' ? 25 : 22,
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: Platform.OS === 'web' ? 16 : 12,
    flexShrink: 0,
  },
  drawerUserDetails: {
    flex: 1,
    minWidth: 0, // Allows text to wrap properly
  },
  drawerUserName: {
    color: '#fff',
    fontSize: Platform.OS === 'web' ? 18 : 16,
    fontWeight: 'bold',
    marginBottom: 6,
    flexWrap: 'wrap',
  },
  drawerPhoneInfo: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 2,
    flexWrap: 'wrap',
  },
  drawerPhoneNumber: {
    color: '#fff',
    fontSize: Platform.OS === 'web' ? 14 : 13,
    marginLeft: 6,
    flexShrink: 1,
    flexWrap: 'wrap',
  },
  drawerMenuContainer: {
    flex: 1,
    paddingTop: 10,
    backgroundColor: '#fff',
    width: '100%',
  },
  drawerMenuItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingVertical: Platform.OS === 'web' ? 18 : 16,
    paddingHorizontal: Platform.OS === 'web' ? 20 : 16,
    borderBottomWidth: 1,
    borderBottomColor: '#f0f0f0',
    width: '100%',
    minHeight: 56,
  },
  drawerMenuItemLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
    minWidth: 0,
  },
  drawerIconContainer: {
    width: Platform.OS === 'web' ? 40 : 36,
    height: Platform.OS === 'web' ? 40 : 36,
    borderRadius: Platform.OS === 'web' ? 20 : 18,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: Platform.OS === 'web' ? 16 : 12,
    flexShrink: 0,
  },
  drawerMenuText: {
    fontSize: Platform.OS === 'web' ? 16 : 15,
    color: '#2c3e50',
    fontWeight: '500',
    flexShrink: 1,
  },
  drawerSeparator: {
    height: 1,
    backgroundColor: '#bdc3c7',
    marginVertical: Platform.OS === 'web' ? 20 : 16,
    marginHorizontal: Platform.OS === 'web' ? 20 : 16,
  },
  drawerAppTipsContainer: {
    paddingHorizontal: Platform.OS === 'web' ? 20 : 16,
    paddingVertical: Platform.OS === 'web' ? 16 : 14,
    marginBottom: 10,
    backgroundColor: '#fff',
    width: '100%',
  },
  drawerAppTipsHeader: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  drawerAppTipsText: {
    fontSize: Platform.OS === 'web' ? 16 : 15,
    color: '#2c3e50',
    fontWeight: '600',
    marginLeft: Platform.OS === 'web' ? 12 : 10,
  },
  drawerDeveloperInfo: {
    paddingHorizontal: 20,
    paddingBottom: 30,
    paddingTop: 20,
    alignItems: 'center',
    backgroundColor: '#f8f9fa',
  },
  drawerDeveloperText: {
    fontSize: 12,
    color: '#7f8c8d',
    marginBottom: 4,
  },
  drawerCompanyName: {
    fontSize: 14,
    color: '#3498db',
    fontWeight: '600',
    marginBottom: 2,
  },
  drawerWebsite: {
    fontSize: 12,
    color: '#3498db',
  },

  // Complaint Screen Styles
  complaintContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  complaintHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  complaintBackButton: {
    padding: 8,
  },
  complaintSaveButton: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    paddingHorizontal: 16,
    paddingVertical: 8,
    borderRadius: 20,
  },
  complaintSaveText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '600',
    marginLeft: 8,
  },
  complaintContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  complaintFieldContainer: {
    marginBottom: 20,
  },
  complaintFieldLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 8,
  },
  complaintDropdownContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
  },
  complaintDropdownInput: {
    flex: 1,
    fontSize: 16,
    color: '#2c3e50',
  },
  complaintTextInput: {
    backgroundColor: '#fff',
    borderBottomWidth: 1,
    borderBottomColor: '#e1e8ed',
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
  },
  complaintMessageInput: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    textAlignVertical: 'top',
    minHeight: 100,
  },

  // Leave Screen Styles
  leaveTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  leaveAddButton: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 16,
  },
  leaveAddText: {
    color: '#fff',
    fontSize: 14,
    fontWeight: '600',
    marginLeft: 4,
  },
  leaveContent: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    paddingHorizontal: 0,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  leaveEmptyText: {
    fontSize: 18,
    color: '#7f8c8d',
    fontWeight: '500',
    marginBottom: 8,
    textAlign: 'center',
  },
  leaveEmptySubtext: {
    fontSize: 14,
    color: '#bdc3c7',
    textAlign: 'center',
    lineHeight: 20,
  },

  // Leave Form Styles
  leaveFormContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  leaveCheckboxContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },
  leaveCheckbox: {
    marginRight: 12,
  },
  leaveCheckboxLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
  },
  leaveFieldContainer: {
    marginBottom: 20,
  },
  leaveFieldLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 8,
  },
  leaveDropdownContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
  },
  leaveDropdownInput: {
    flex: 1,
    fontSize: 16,
    color: '#2c3e50',
  },
  leaveTextInput: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
  },
  leaveApplyButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 30,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  leaveApplyButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },

  // Travelling Screen Styles
  travellingTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  travellingAddButton: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: 'rgba(255, 255, 255, 0.2)',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 16,
  },
  travellingAddText: {
    color: '#fff',
    fontSize: 14,
    fontWeight: '600',
    marginLeft: 4,
  },
  travellingContent: {
    flex: 1,
    paddingVertical: 10,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  travellingEmptyText: {
    fontSize: 18,
    color: '#7f8c8d',
    fontWeight: '500',
    marginBottom: 8,
    textAlign: 'center',
  },
  travellingEmptySubtext: {
    fontSize: 14,
    color: '#bdc3c7',
    textAlign: 'center',
    lineHeight: 20,
  },

  // Travelling Form Styles
  travellingFormContent: {
    flex: 1,
    paddingHorizontal: 20,
    paddingTop: 20,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  travellingFieldContainer: {
    marginBottom: 20,
  },
  travellingFieldLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 8,
  },
  travellingDropdownContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
  },
  travellingDropdownInput: {
    flex: 1,
    fontSize: 16,
    color: '#2c3e50',
  },
  travellingTextInput: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
  },
  travellingApplyButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 30,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  travellingApplyButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },

  // Updated Travelling Form Styles
  travellingFormInput: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ddd',
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    marginBottom: 16,
  },
  travellingAttachmentLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 8,
  },
  travellingChooseFileButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingHorizontal: 20,
    paddingVertical: 12,
    alignSelf: 'flex-start',
  },
  travellingChooseFileText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: '500',
  },
  travellingSubmitButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 30,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  travellingSubmitButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },

  // Add Customer Screen Styles
  customerContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  customerHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  customerBackButton: {
    padding: 8,
  },
  customerTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
  customerContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  customerFieldContainer: {
    marginBottom: 20,
  },
  customerInput: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ddd',
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
  },
  customerTextArea: {
    minHeight: 100,
    textAlignVertical: 'top',
  },
  customerSubmitButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 30,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  customerSubmitButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },

  // Create AMC Screen Styles
  amcContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  amcHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  amcBackButton: {
    padding: 8,
  },
  amcTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
  amcContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  amcFieldContainer: {
    marginBottom: 20,
  },
  amcSectionLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 12,
    textAlign: 'center',
  },
  amcLabel: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 8,
  },
  amcDropdownContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ddd',
    paddingHorizontal: 16,
    paddingVertical: 12,
  },
  amcDropdownText: {
    fontSize: 16,
    color: '#2c3e50',
    flex: 1,
  },
  amcPlaceholderText: {
    color: '#999',
  },
  amcDropdownList: {
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ddd',
    marginTop: 4,
    elevation: 3,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
  },
  amcDropdownItem: {
    paddingHorizontal: 16,
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#f0f0f0',
  },
  amcDropdownItemText: {
    fontSize: 16,
    color: '#2c3e50',
  },
  amcDateRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  amcDateButton: {
    flex: 1,
    backgroundColor: '#fff',
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#ddd',
    paddingHorizontal: 16,
    paddingVertical: 12,
    marginHorizontal: 4,
  },
  amcDateButtonText: {
    fontSize: 16,
    color: '#2c3e50',
    textAlign: 'center',
  },
  amcUnderlineInput: {
    fontSize: 16,
    color: '#2c3e50',
    borderBottomWidth: 1,
    borderBottomColor: '#ddd',
    paddingVertical: 8,
  },
  amcNotesInput: {
    minHeight: 80,
    textAlignVertical: 'top',
  },
  amcSubmitButton: {
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 16,
    alignItems: 'center',
    marginTop: 20,
    marginBottom: 30,
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  amcSubmitButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: '600',
  },

  // View Attendance Screen Styles
  attendanceContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  attendanceHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  attendanceBackButton: {
    padding: 8,
  },
  attendanceTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
  attendanceContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  attendanceCalendarCard: {
    backgroundColor: '#fff',
    borderRadius: 12,
    padding: 20,
    marginBottom: 20,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  attendanceCalendarNav: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginBottom: 20,
  },
  attendanceMonthYear: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#2c3e50',
  },
  attendanceNavRight: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  attendanceWeeksButton: {
    borderWidth: 1,
    borderColor: '#2c3e50',
    borderRadius: 20,
    paddingHorizontal: 12,
    paddingVertical: 6,
    marginRight: 12,
  },
  attendanceWeeksButtonText: {
    fontSize: 14,
    color: '#2c3e50',
    fontWeight: '500',
  },
  attendanceWeekDays: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 10,
  },
  attendanceWeekDayText: {
    flex: 1,
    fontSize: 13,
    color: '#7f8c8d',
    fontWeight: '600',
    textAlign: 'center',
  },
  attendanceCalendarGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  attendanceCalendarDay: {
    width: '14.2857%',
    paddingVertical: 6,
    justifyContent: 'center',
    alignItems: 'center',
    marginVertical: 4,
  },
  attendanceCalendarDayInactive: {
    opacity: 0.4,
  },
  attendanceCalendarDaySelected: {
    backgroundColor: '#3498db',
  },
  attendanceCalendarDayText: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
  },
  attendanceCalendarDayTextInactive: {
    color: '#bdc3c7',
  },
  attendanceCalendarDayTextSelected: {
    color: '#fff',
    fontWeight: 'bold',
  },
  attendanceSummaryCard: {
    backgroundColor: '#34495e',
    borderRadius: 12,
    padding: 20,
    marginBottom: 20,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  attendanceSummaryTitle: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#fff',
    textAlign: 'center',
    marginBottom: 20,
  },
  attendanceLegend: {
    marginBottom: 20,
  },
  attendanceLegendItem: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8,
  },
  attendanceLegendCircle: {
    width: 12,
    height: 12,
    borderRadius: 6,
    marginRight: 12,
  },
  attendanceLegendText: {
    flex: 1,
    fontSize: 14,
    color: '#ecf0f1',
  },
  attendanceLegendCount: {
    fontSize: 14,
    color: '#ecf0f1',
    fontWeight: 'bold',
    minWidth: 20,
    textAlign: 'right',
  },
  attendanceChartContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 200,
  },
  attendanceChartPlaceholder: {
    width: 150,
    height: 150,
    borderRadius: 75,
    backgroundColor: '#2c3e50',
    justifyContent: 'center',
    alignItems: 'center',
  },
  attendanceChartPlaceholderText: {
    fontSize: 12,
    color: '#bdc3c7',
    marginTop: 8,
    textAlign: 'center',
  },

  // Tips Modal Styles
  tipsModalOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  tipsModalContainer: {
    backgroundColor: '#fff',
    borderRadius: 16,
    width: '100%',
    maxWidth: 400,
    maxHeight: '80%',
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.25,
    shadowRadius: 8,
    elevation: 8,
  },
  tipsModalHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 20,
    paddingVertical: 16,
    borderBottomWidth: 1,
    borderBottomColor: '#f0f0f0',
  },
  tipsModalTitle: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#2c3e50',
  },
  tipsLanguageSelector: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#f8f9fa',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 8,
    borderWidth: 1,
    borderColor: '#e1e8ed',
  },
  tipsLanguageText: {
    fontSize: 14,
    color: '#2c3e50',
    marginRight: 8,
  },
  tipsModalContent: {
    paddingHorizontal: 20,
    paddingVertical: 16,
    maxHeight: 400,
  },
  tipsModalTipItem: {
    marginBottom: 16,
  },
  tipsModalTipNumber: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#2c3e50',
    marginBottom: 4,
  },
  tipsModalTipContent: {
    fontSize: 14,
    color: '#34495e',
    lineHeight: 20,
  },
  tipsModalFooter: {
    paddingHorizontal: 20,
    paddingVertical: 16,
    borderTopWidth: 1,
    borderTopColor: '#f0f0f0',
    alignItems: 'flex-end',
  },
  tipsModalCloseButton: {
    paddingHorizontal: 20,
    paddingVertical: 10,
  },
  tipsModalCloseButtonText: {
    fontSize: 16,
    color: '#9b59b6',
    fontWeight: '600',
  },
  workCheckModalOverlay: {
    flex: 1,
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
    justifyContent: 'center',
    alignItems: 'center',
    paddingHorizontal: 20,
  },
  workCheckModalContainer: {
    backgroundColor: '#fff',
    borderRadius: 12,
    width: '100%',
    maxWidth: 400,
    padding: 20,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.25,
    shadowRadius: 8,
    elevation: 8,
  },
  workCheckModalTitle: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#2c3e50',
    marginBottom: 16,
    textAlign: 'center',
  },
  workCheckModalInput: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ddd',
    borderRadius: 8,
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    minHeight: 120,
    marginBottom: 20,
    textAlignVertical: 'top',
  },
  workCheckModalButtons: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    gap: 12,
  },
  workCheckCloseButton: {
    flex: 1,
    backgroundColor: '#e74c3c',
    borderRadius: 8,
    paddingVertical: 12,
    alignItems: 'center',
  },
  workCheckCloseButtonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
  workCheckSubmitButton: {
    flex: 1,
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 12,
    alignItems: 'center',
  },
  workCheckSubmitButtonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },

  // Routine Maintenance Screen Styles
  routineMaintenanceContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  routineMaintenanceHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  routineMaintenanceBackButton: {
    padding: 8,
  },
  routineMaintenanceTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  routineMaintenanceHeaderSpacer: {
    width: 40,
  },
  routineMaintenanceContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  routineMaintenanceItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    backgroundColor: '#fff',
    borderRadius: 12,
    paddingVertical: 24,
    paddingHorizontal: 16,
    marginBottom: 16,
    minHeight: 70,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  routineMaintenanceItemLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
  routineMaintenanceIconContainer: {
    width: 40,
    height: 40,
    borderRadius: 20,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 16,
  },
  routineMaintenanceItemText: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
  },

  // AMC List Screen Styles
  amcListContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  amcListHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  amcListBackButton: {
    padding: 8,
  },
  amcListTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  amcListSearchButton: {
    padding: 8,
  },
  amcListContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  amcListItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    backgroundColor: '#fff',
    borderRadius: 12,
    paddingVertical: 20,
    paddingHorizontal: 16,
    marginBottom: 12,
    minHeight: 80,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  amcListItemLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
  amcListIconContainer: {
    width: 50,
    height: 50,
    borderRadius: 25,
    backgroundColor: '#f8f9fa',
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 16,
    position: 'relative',
  },
  amcListWrenchIcon: {
    position: 'absolute',
    bottom: 2,
    right: 2,
  },
  amcListItemDetails: {
    flex: 1,
  },
  amcListItemTitle: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    marginBottom: 4,
  },
  amcListItemSubtitle: {
    fontSize: 14,
    color: '#7f8c8d',
  },
  amcListStatusText: {
    fontWeight: '600',
  },

  // Customers Screen Styles
  customersContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  customersHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  customersBackButton: {
    padding: 8,
  },
  customersTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  customersSearchButton: {
    padding: 8,
  },
  customersContent: {
    flex: 1,
    backgroundColor: '#fefefe',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  customersItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingVertical: 20,
    paddingHorizontal: 12,
    minHeight: 70,
    borderBottomWidth: 1,
    borderBottomColor: '#f0f0f0',
  },
  customersItemLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
  customersIconContainer: {
    width: 30,
    height: 30,
    borderRadius: 15,
    backgroundColor: '#f8f9fa',
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 16,
  },
  customersItemText: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: '500',
    flex: 1,
  },
  customersSearchContainer: {
    backgroundColor: '#fff',
    paddingHorizontal: 12,
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#e0e0e0',
  },
  customersSearchInput: {
    backgroundColor: '#f8f9fa',
    borderRadius: 8,
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },
  amcListSearchContainer: {
    backgroundColor: '#fff',
    paddingHorizontal: 12,
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#e0e0e0',
  },
  amcListSearchInput: {
    backgroundColor: '#f8f9fa',
    borderRadius: 8,
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },

  // Tickets Screen Styles
  ticketsContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  ticketsHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  ticketsBackButton: {
    padding: 8,
  },
  ticketsTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  ticketsHeaderActions: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  ticketsSearchButton: {
    padding: 8,
    marginRight: 8,
  },
  ticketsFilterButton: {
    padding: 8,
  },
  ticketsSearchContainer: {
    backgroundColor: '#fff',
    paddingHorizontal: 12,
    paddingVertical: 12,
    borderBottomWidth: 1,
    borderBottomColor: '#e0e0e0',
  },
  ticketsSearchInput: {
    backgroundColor: '#f8f9fa',
    borderRadius: 8,
    paddingHorizontal: 16,
    paddingVertical: 12,
    fontSize: 16,
    color: '#2c3e50',
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },
  ticketsContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 20,
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  ticketsItem: {
    backgroundColor: '#fff',
    borderRadius: 12,
    marginBottom: 16,
    minHeight: 90,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  ticketsMainRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    paddingVertical: 20,
    paddingHorizontal: 16,
  },
  ticketsMainRowLeft: {
    flexDirection: 'row',
    alignItems: 'center',
    flex: 1,
  },
  ticketsNotificationIcon: {
    width: 40,
    height: 40,
    borderRadius: 20,
    backgroundColor: '#f8f9fa',
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 16,
  },
  ticketsMainRowContent: {
    flex: 1,
  },
  ticketsTitleText: {
    fontSize: 16,
    color: '#2c3e50',
    fontWeight: 'bold',
    marginBottom: 4,
  },
  ticketsDateTimeText: {
    fontSize: 14,
    fontWeight: '500',
  },
  ticketsExpandedContent: {
    paddingHorizontal: 16,
    paddingBottom: 16,
    borderTopWidth: 1,
    borderTopColor: '#f0f0f0',
  },
  ticketsDetailText: {
    fontSize: 14,
    color: '#2c3e50',
    marginBottom: 8,
    lineHeight: 20,
  },
  ticketsActionButtons: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 16,
    gap: 12,
  },
  ticketsGoToDetailsButton: {
    flex: 1,
    backgroundColor: '#e74c3c',
    borderRadius: 8,
    paddingVertical: 12,
    alignItems: 'center',
  },
  ticketsGoToDetailsButtonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
  ticketsAssignButton: {
    flex: 1,
    backgroundColor: '#3498db',
    borderRadius: 8,
    paddingVertical: 12,
    alignItems: 'center',
  },
  ticketsAssignButtonText: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },

  // Mark Attendance Screen Styles
  markAttendanceContainer: {
    flex: 1,
    backgroundColor: '#f8f9fa',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? WEB_SCREEN_HEIGHT : SCREEN_HEIGHT,
  },
  markAttendanceHeader: {
    backgroundColor: '#3498db',
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 12,
    paddingTop: Platform.OS === 'web' ? 16 : Math.max(StatusBar.currentHeight || 0, 20) + 5,
    paddingBottom: Platform.OS === 'web' ? 14 : 12,
  },
  markAttendanceBackButton: {
    padding: 8,
  },
  markAttendanceTitle: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
    flex: 1,
    textAlign: 'center',
    marginHorizontal: 20,
  },
  markAttendanceHeaderSpacer: {
    width: 40,
  },
  markAttendanceContent: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 40,
    alignItems: 'center',
    width: '100%',
    alignSelf: 'stretch',
    minHeight: Platform.OS === 'web' ? 'auto' : SCREEN_HEIGHT * 0.7,
  },
  markAttendanceCameraPreview: {
    width: '100%',
    height: 300,
    backgroundColor: '#fff',
    borderRadius: 12,
    borderWidth: 2,
    borderColor: '#2c3e50',
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 40,
    position: 'relative',
  },
  markAttendancePreviewText: {
    fontSize: 16,
    color: '#7f8c8d',
    fontWeight: '500',
    textAlign: 'center',
  },
  markAttendanceSuccessIcon: {
    position: 'absolute',
    top: 20,
    right: 20,
  },
  markAttendanceButtons: {
    width: '100%',
    gap: 16,
  },
  markAttendanceTakeSelfieButton: {
    backgroundColor: '#3498db',
    borderRadius: 12,
    paddingVertical: 16,
    alignItems: 'center',
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  markAttendanceSubmitButton: {
    backgroundColor: '#3498db',
    borderRadius: 12,
    paddingVertical: 16,
    alignItems: 'center',
    shadowColor: '#3498db',
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowOpacity: 0.3,
    shadowRadius: 8,
    elevation: 4,
  },
  markAttendanceButtonDisabled: {
    backgroundColor: '#bdc3c7',
    shadowOpacity: 0.1,
  },
  markAttendanceButtonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },

  // Mobile-specific alignment fixes
  mobileContainer: {
    flex: 1,
    width: '100%',
    alignSelf: 'stretch',
  },
  mobileScrollView: {
    flex: 1,
    width: '100%',
  },
  mobileContent: {
    flexGrow: 1,
    width: '100%',
    alignSelf: 'stretch',
  },
  mobileGrid: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
    width: '100%',
    alignSelf: 'stretch',
  },
  mobileCard: {
    width: '48%',
    alignSelf: 'stretch',
  },
  mobileButton: {
    width: '100%',
    alignSelf: 'stretch',
  },
  mobileInput: {
    width: '100%',
    alignSelf: 'stretch',
  },
});
